<%= form_for(@comment, :remote => true, :url => comments_path(:sID => @spaeti.id)) do |f| %>
  <%= f.error_messages %>
  <%= f.label :comment, "Kommentar schreiben" %><br />
  <%= f.text_area :comment, :rows => 5, :cols => 35 %>
  <p><%= f.submit "Abschicken" %></p>
<% end %>
