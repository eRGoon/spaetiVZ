<!DOCTYPE html>
<html lang="de">
  <head>
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta charset="utf-8">
    <title>spaetiVZ</title>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag 'jquery' %>
    <%= javascript_include_tag 'rails' %>
    <%= javascript_include_tag 'http://maps.google.com/maps/api/js?sensor=false' %>
    <%= javascript_include_tag 'raphael-min' %>
    <%= javascript_include_tag 'functions' %>
    <%= yield :head %>
  </head>
  <body>
    <div id="container">
      <div id="flash_notice"></div>
      <header>
          <% title "spaetiVZ" %>
          <%= content_tag :h1, yield(:title) if show_title? %>
          <div id="search">
            <%= form_tag spaetis_path , :method => 'get', :remote => true, :class => "search" do %>
                  <%= text_field_tag :search, params[:search], :placeholder => "alle" %>
                  <%= submit_tag "Suchen", :name => nil %>
            <% end %>
            <%= link_to "", new_spaeti_path, :remote => true, :class => "rp new", :title => "Spätkauf erstellen" %>
            <!--<a href="" class="rp new" title="Spätkauf hinzufügen"></a>-->
          </div>
      </header>
      <%= yield %>
    </div>
  </body>
</html>
